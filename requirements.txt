Flask==3.1.3
gunicorn==26.0.0
opencv-python-headless==4.11.0.86
numpy==2.4.6
python-dotenv==1.0.1

# Optional dependencies (uncomment if needed)
# twilio>=9.0.0       # SMS alerts
# WMI>=1.5.1          # Windows login monitoring (Windows only)
# pywin32>=306        # Required by WMI on Windows
